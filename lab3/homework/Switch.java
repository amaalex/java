package lab3.homework;

/**
 * Switch class which extends Node, with specific attribures, constructor, getters and setters
 */
public class Switch extends Node {

    Switch(String name, String macAddress, String location) {
        setName(name);
        setMacAddress(macAddress);
        setLocation(location);
    }
}
